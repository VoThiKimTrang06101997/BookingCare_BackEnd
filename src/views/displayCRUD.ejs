<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Get CRUD</title>
    <style>
        table,
        th,
        td {
            border: 1px solid black;
            border-collapse: collapse;
        }
        .btn-edit {
            background-color: green;
            color: white;
            outline: none;
            border: none;
            height: 25px;
            width: 60px;
            border-radius: 3px;
            cursor: pointer;
        }
        .btn-delete{
            background-color: #ff0023;
            color: white;
            outline: none;
            border: none;
            height: 25px;
            width: 60px;
            border-radius: 3px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <table style="width:100%">
        <h1 style="color: red; text-align: center;">User</h1>
        <tr style="color: blue">
            <th>Email</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Address</th>
            <th>Action</th>
        </tr>

        <% for(let i=0; i<dataTable.length; i++) {%>
            <tr style="text-align: center;">
                <td>
                    <%=dataTable[i].email %>
                </td>
                <td>
                    <%=dataTable[i].firstName %>
                </td>
                <td>
                    <%=dataTable[i].lastName %>
                </td>
                <td>
                    <%=dataTable[i].address %>
                </td>
                <td>
                    <a class="btn-edit" type="button" href="/edit-crud?id=<%=dataTable[i].id%>">Edit</a>
                    <a class="btn-delete" type="button" href="/delete-crud?id=<%=dataTable[i].id%>">Delete</a>
                </td>
            </tr>
            <%}%>
    </table>
</body>

</html>